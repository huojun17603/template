<?xml version="1.0" encoding="UTF-8"?>
<!-- 系统动态配置项，必须直接通过接口修改配置，修改后直接有效无需系统重启；注意：必须系统启动完成后才有效 -->
<!-- XML中配置的是默认值，如果数据库中有值则优先使用数据库中的值 -->
<group name="系统基本参数" >
    <config key="OUT-REQUEST" value="1" driver="^[01]$" docs="请求参数打印；0：不打印；1：打印"></config>
</group>

